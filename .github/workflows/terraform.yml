name: Terraform

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  terraform:
    name: Terraform
    runs-on: ubuntu-latest


    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.11.4

      - name: Terraform Init
        run: terraform init
        
      - name: Clean workspace
        run: rm -rf .terraform/ || true  

      - name: Terraform Format
        run: terraform fmt -check -recursive

      - name: Generate Terraform Docs
        uses: terraform-docs/gh-actions@v1
        with:
          working-dir: ./modules/resource_group
          output-file: README.md
          output-method: inject
          git-push: "true"
      # - name: Install terraform-docs
      #   run: |
      #     curl -sSLo terraform-docs.tar.gz https://github.com/terraform-docs/terraform-docs/releases/download/${TFDOCS_VERSION}/terraform-docs-${TFDOCS_VERSION}-linux-amd64.tar.gz
      #     tar -xzf terraform-docs.tar.gz
      #     chmod +x terraform-docs
      #     sudo mv terraform-docs /usr/local/bin/
          
      # - name: Generate Terraform Docs
      #   run: |
      #     terraform-docs markdown table --output-file README.md --output-mode inject .
      #     git config --global user.name "GitHub Actions"
      #     git config --global user.email "actions@github.com"
      #     git add ./modules/resource_group/README.md
      #     git commit -m "Update Terraform docs" || echo "No documentation changes to commit"
      #     git push || echo "No changes to push"


      # - name: Terraform Plan
      #   run: terraform plan -input=false

      - name: Terraform Validate
        run: terraform validate
